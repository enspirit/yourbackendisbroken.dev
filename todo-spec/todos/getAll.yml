---
name: |-
  Todos

url: |-
  /todos

services:
- method: |-
    GET

  description: |-
    Returns the collection of todos

  input_schema: |-
    .

  output_schema: |-
    [TodoFull]

  error_schema: |-
    .

  examples:

    - description: |-
        when requested
      expected:
        content_type: application/json
        status: 200
      assert:
        - size(5)
        - "pathFD('0', id: 1, description: 'one')"
