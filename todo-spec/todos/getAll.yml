---
name: |-
  Todos

url: |-
  /todos

services:
- method: |-
    GET

  description: |-
    Returns the collection of todos

  input_schema: |-
    Any

  output_schema: |-
    [{
      id: Integer
      description: String
    }]

  error_schema: |-
    Any

  examples:

    - description: |-
        when requested
      expected:
        content_type: application/json
        status: 200
      assert:
        - size(6)
