---
name: |-
  Todo

url: |-
  /todos/{id}

services:
- method: |-
    GET

  description: |-
    Returns the specified todo

  input_schema: |-
    {
      id: Integer
    }

  output_schema: |-
    {
      id: Integer
      description: String
    }

  error_schema: |-
    .

  examples:

    - description: |-
        when requested
      params:
        id: 6
      expected:
        content_type: application/json
        status: 200
      assert:
        - "pathFD('', id: 6, description: 'Use counter examples')"
